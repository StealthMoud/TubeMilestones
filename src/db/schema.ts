import type {
  AnalyticsSummary,
  AppMetadata,
  Channel,
  ChannelSnapshot,
  CustomGoal,
  DailyAnalytics,
  ManualMetrics,
  MilestoneState,
} from '../domain/models';

export const DATABASE_NAME = 'TubeMilestones';
export const DATABASE_SCHEMA_VERSION = 1;

export type {
  AnalyticsSummary,
  AppMetadata,
  Channel,
  ChannelSnapshot,
  CustomGoal,
  DailyAnalytics,
  ManualMetrics,
  MilestoneState,
};
